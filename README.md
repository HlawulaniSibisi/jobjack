"# jobjack" 


Runing instructions

Building and running docker container for back end(Run the following commands)
$docker build -t jobjack/node-web-app . //to build
$docker run -p 8082:8081 -d jobjack/node-web-app //to run


If you want to run using node js
$node server.js
